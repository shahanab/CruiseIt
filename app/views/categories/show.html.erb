<!-- <p id="notice"><%= notice %></p> -->
<div class="categories-show">
<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<ul>
	<% @category.pois.each do |poi|  %>
		<li><%= poi.description %></li>
	<% end %>
</ul>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
</div>