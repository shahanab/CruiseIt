<%= form_for(@itinerary) do |f| %>
  <% if @itinerary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@itinerary.errors.count, "error") %> prohibited this itinerary from being saved:</h2>

      <ul>
      <% @itinerary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="field">
    <strong> Itinerary </strong><%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
</div>

<div class="row">
  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
</div>
<div class="row">
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_select :end_date %>
  </div>
</div>
<div class="row">
  <%= f.fields_for :itinerary_destination do |g| %>
    <%= g.collection_select :destination_id, Destination.all, :id, :name %>
</div>
    <% end %>
 <div class="row">
  <div class="actions">

    <%= f.submit %>
  </div>
</div>
<% end %>
