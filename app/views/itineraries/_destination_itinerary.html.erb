<%= form_for(ItineraryDestination.new, url: {method: "create"}, html: {multipart: true} ) do |f| %>
	<%= f.label :destination_id%>
	<%= f.collection_select :destination_id, Destination.all, :id, :name %>
	<%= f.submit 'Select Destination' %>
<% end %>